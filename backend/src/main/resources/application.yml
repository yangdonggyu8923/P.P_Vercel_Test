
spring:
    main:
        allow-bean-definition-overriding: true
        web-application-type: reactive
    application:
        name: rootapp
    data:
        mongodb:
            uri: mongodb://root:root@101.79.10.130:27017/lawmatedb?retryWrites=true&w=majority&authSource=admin
    sql:
        init:
            mode: always
            encoding: UTF-8
logging:
    level:
        org:
            hibernate:
                SQL: debug
                type: trace

jwt:
    secret: ydglawmatesite
    expiration: 86400000
